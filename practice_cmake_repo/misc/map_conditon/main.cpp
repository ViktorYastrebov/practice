#include <iostream>

#include <map>
#include <string>
#include <functional>
//std::plus, std::minus

//see http://www.boost.org/doc/libs/1_48_0/doc/html/lambda/extending.html
//
//it useless because - switch => lookup table(O(1)) meanwhile map<T>.find (O(logn))

int main() {

 return 0;
}